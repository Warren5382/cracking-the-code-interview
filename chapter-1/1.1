QUESTION:
Implement an algorithm to determine if a string has all unique characters.

SOLUTION:
bool check_unique_string(std::string& str) {
	bool* char_set = new char[256];
	for (int i = 0; i < 256; ++i) {
		char_set[i] = false;
	}
	for (int i = 0; i < str.length(); ++i) {
		int char_to_int = str[i];
		if (char_set[char_to_int]) {
			return false;
		}
		char_set[char_to_int] = true;
	}
	return true;
}
		
